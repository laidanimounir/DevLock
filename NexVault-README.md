# 🔐 NexVault
### خزنة المطور الاحترافية — الجيل القادم

> **الفكرة الأساسية:**
> لا يوجد في السوق أداة تجمع بين إدارة مشاريع الفريلانسر وحفظ بيانات الدخول المشفرة في مكان واحد.
> NexVault يملأ هذه الفجوة.

---

## 📌 ما هو NexVault؟

نظام شخصي احترافي مبني خصيصاً للمطور الفريلانسر — يجمع في مكان واحد:
- إدارة مشاريع العملاء كاملاً
- حفظ بيانات الدخول بتشفير عسكري
- مراقبة صحة المشاريع تلقائياً
- الفواتير والمدفوعات والتذكيرات
- تزامن فوري بين الموبايل والويب

---

## 🚀 لماذا NexVault مختلف؟

| الأدوات الموجودة | NexVault |
|-----------------|---------|
| Notion / Monday → يدير مشاريع فقط | ✅ مشاريع + باسوردات |
| Bitwarden / 1Password → باسوردات فقط | ✅ باسوردات + سياق المشروع |
| لا أحد → Health Check للمشاريع | ✅ مراقبة تلقائية |
| لا أحد → ping لـ Supabase المجاني | ✅ يمنع التوقف التلقائي |
| تكلفة شهرية | ✅ تكلفة صفر |

---

## 🏗️ المعمارية العامة

```
┌─────────────────────────────────────────┐
│             المستخدم (أنت)              │
└──────────┬───────────────┬──────────────┘
           │               │
    ┌──────▼──────┐  ┌─────▼────────┐
    │  Expo Web   │  │ Expo Mobile  │
    │ (المتصفح)  │  │ (iOS/Android)│
    └──────┬──────┘  └─────┬────────┘
           │               │
    ┌──────▼───────────────▼──────────┐
    │            Supabase             │
    │         (على Oracle)            │
    ├─────────────────────────────────┤
    │  Auth  │  Database  │  Storage  │
    │        │  Realtime  │           │
    └─────────────────────────────────┘
```

---

## 🛠️ Stack التقنيات

### Frontend / Mobile
| التقنية | الاستخدام | السبب |
|---------|-----------|-------|
| **Expo (React Native)** | الويب + iOS + Android | كود واحد للكل |
| **NativeWind** | التصميم | Tailwind على الموبايل |
| **Zustand** | إدارة الحالة | خفيف وبسيط |
| **React Query** | جلب البيانات | cache وتزامن ذكي |
| **React Navigation** | التنقل | معيار Expo |

### Backend
| التقنية | الاستخدام | السبب |
|---------|-----------|-------|
| **Supabase** | DB + Auth + Storage | مجاني على Oracle |
| **Supabase Realtime** | تزامن فوري | يحدث تلقائياً |
| **Supabase Storage** | الصور والملفات | مدمج ومجاني |
| **Supabase Edge Functions** | Health Check و ping | serverless مجاني |

### الأمان والتشفير
| التقنية | الاستخدام |
|---------|-----------|
| **AES-256** | تشفير كلمات السر |
| **Argon2** | تشفير PIN الرئيسي |
| **PBKDF2** | تحويل PIN لمفتاح تشفير |
| **TOTP** | التحقق بخطوتين |
| **Shamir's Secret Sharing** | تقسيم المفتاح لمنع الضياع |

### الإيميل (مجاني)
| الخدمة | الحد المجاني |
|--------|-------------|
| **Resend** | 3,000 إيميل/شهر |
| **Brevo** | 300 إيميل/يوم |

---

## 🔒 نظام الأمان — ثلاث طبقات

```
الدخول للنظام:

طبقة ١ ── إيميل + باسورد قوي
                ↓
طبقة ٢ ── TOTP كود من Google Authenticator
          (يتغير كل 30 ثانية — بدون SMS — مجاني)
                ↓
طبقة ٣ ── PIN التشفير الرئيسي
          (يعيش فقط في ذاكرة الجهاز)
                ↓
         ✅ كل بياناتك تظهر
```

---

## 🗝️ نظام Shamir's Secret Sharing

**حل مشكلة نسيان المفتاح الرئيسي:**

```
مفتاحك يُقسم لـ ٣ أجزاء:

جزء ١ ──→ تحفظه أنت (ورقة / حفظ شخصي)
جزء ٢ ──→ ملف مشفر على جهازك
جزء ٣ ──→ في Supabase مشفر

القاعدة: تحتاج أي ٢ من ٣ للاسترجاع
```

**السيناريوهات:**

| المشكلة | الحل |
|---------|------|
| نسيت المفتاح | جزء الجهاز + جزء Supabase ✅ |
| ضاع الجهاز | جزءك الشخصي + جزء Supabase ✅ |
| اخترق Supabase | جزء واحد فقط = لا يكفي ✅ |

---

## 🗄️ قاعدة البيانات الكاملة

### جدول المشاريع
```sql
projects (
  id              UUID PRIMARY KEY,
  client_name     TEXT NOT NULL,
  project_name    TEXT NOT NULL,
  type            TEXT,           -- موبايل / ويب / متنوع
  status          TEXT,           -- نشط / متوقف / مكتمل / صيانة
  technologies    TEXT[],         -- [Firebase, Supabase, React Native...]
  notes           TEXT,
  thumbnail_url   TEXT,
  contract_value  DECIMAL,        -- قيمة المشروع
  paid_amount     DECIMAL,        -- المبلغ المدفوع
  payment_status  TEXT,           -- مدفوع / جزئي / معلق
  domain_expiry   DATE,           -- تاريخ انتهاء الدومين
  hosting_expiry  DATE,           -- تاريخ انتهاء الاستضافة
  last_contact    DATE,           -- آخر تواصل مع العميل
  created_at      TIMESTAMP,
  updated_at      TIMESTAMP
)
```

### جدول بيانات الدخول
```sql
credentials (
  id            UUID PRIMARY KEY,
  project_id    UUID REFERENCES projects(id),
  service       TEXT,           -- Supabase / Firebase / Hosting / Domain
  email         TEXT,           -- إيميل نص عادي
  password_enc  TEXT,           -- باسورد مشفر AES-256
  iv            TEXT,           -- IV عشوائي لكل باسورد
  url           TEXT,           -- رابط لوحة التحكم
  plan          TEXT,           -- Free / Pro / Blaze
  extra_enc     TEXT,           -- ملاحظات سرية مشفرة
  created_at    TIMESTAMP
)
```

### جدول معلومات العميل
```sql
clients (
  id            UUID PRIMARY KEY,
  project_id    UUID REFERENCES projects(id),
  phone         TEXT,
  whatsapp      TEXT,
  email         TEXT,
  preferred_contact TEXT,       -- واتساب / إيميل / هاتف
  personal_notes TEXT,          -- ملاحظات شخصية عن العميل
  created_at    TIMESTAMP
)
```

### جدول الفواتير
```sql
invoices (
  id            UUID PRIMARY KEY,
  project_id    UUID REFERENCES projects(id),
  amount        DECIMAL,
  status        TEXT,           -- مدفوع / معلق / متأخر
  due_date      DATE,
  paid_date     DATE,
  notes         TEXT,
  created_at    TIMESTAMP
)
```

### جدول الملفات والصور
```sql
attachments (
  id            UUID PRIMARY KEY,
  project_id    UUID REFERENCES projects(id),
  file_url      TEXT,
  file_type     TEXT,           -- صورة / PDF / أخرى
  file_name     TEXT,
  description   TEXT,
  created_at    TIMESTAMP
)
```

### جدول Health Check
```sql
health_checks (
  id            UUID PRIMARY KEY,
  project_id    UUID REFERENCES projects(id),
  service       TEXT,           -- Supabase / Website / Firebase
  status        TEXT,           -- up / down / warning
  last_check    TIMESTAMP,
  response_time INTEGER,        -- بالميلي ثانية
  notes         TEXT
)
```

### جدول سجل النشاط
```sql
activity_log (
  id            UUID PRIMARY KEY,
  action        TEXT,           -- view_password / add_project / edit...
  project_id    UUID,
  device        TEXT,           -- موبايل / ويب
  created_at    TIMESTAMP
)
```

---

## 🎨 شاشات التطبيق

```
📱 NexVault
│
├── 🔐 الدخول
│   ├── إيميل + باسورد
│   ├── TOTP كود
│   └── PIN التشفير
│
├── 🏠 الرئيسية (Dashboard)
│   ├── إحصائيات سريعة
│   │   ├── عدد المشاريع النشطة
│   │   ├── إجمالي الدخل الشهري
│   │   ├── مشاريع تحتاج متابعة
│   │   └── تنبيهات (دومين ينتهي، باسورد قديم...)
│   ├── قائمة المشاريع
│   ├── بحث سريع
│   └── ➕ مشروع جديد
│
├── 📁 تفاصيل المشروع
│   ├── معلومات العميل
│   ├── بيانات الدخول (مع 👁️ و 📋)
│   ├── الفواتير والمدفوعات
│   ├── الصور والملفات
│   ├── Health Status ✅/⚠️/❌
│   └── ملاحظات
│
├── ➕ إضافة / تعديل مشروع
│   ├── بيانات العميل
│   ├── التقنيات (اختيار من قائمة)
│   ├── بيانات الدخول لكل خدمة
│   ├── قيمة المشروع والدفعات
│   └── تواريخ انتهاء الدومين والاستضافة
│
├── 📊 الإحصائيات
│   ├── دخل كل شهر
│   ├── أكثر التقنيات استخداماً
│   ├── مشاريع حسب الحالة
│   └── عملاء لم تتواصل معهم منذ فترة
│
└── ⚙️ الإعدادات
    ├── تغيير PIN
    ├── إدارة TOTP
    ├── النسخ الاحتياطي
    └── استعادة Shamir
```

---

## ✨ الميزات الكاملة

### 🔐 الأمان
- ✅ ثلاث طبقات حماية عند الدخول
- ✅ AES-256 بـ IV مختلف لكل باسورد
- ✅ Shamir's Secret Sharing لمنع ضياع المفتاح
- ✅ Timeout تلقائي بعد 5 دقائق عدم نشاط
- ✅ منع Screenshot على الموبايل
- ✅ إخفاء المحتوى عند تبديل التطبيقات
- ✅ سجل كامل لكل عملية عرض أو تعديل

### 📋 إدارة المشاريع
- ✅ إضافة مشروع جديد بدون كود
- ✅ بحث فوري في كل المشاريع
- ✅ فلترة بالتقنية أو الحالة أو العميل
- ✅ صور وملفات متعددة لكل مشروع
- ✅ تتبع حالة المشروع (تطوير / تسليم / صيانة)
- ✅ ملاحظات شخصية عن كل عميل

### 💰 المالية
- ✅ تتبع قيمة كل مشروع
- ✅ تتبع الدفعات (مقدم / متبقي)
- ✅ تذكير بالفواتير المتأخرة
- ✅ إحصائيات الدخل الشهري والسنوي

### 🤖 الأتمتة الذكية
- ✅ Health Check تلقائي لكل مشروع
- ✅ ping تلقائي لـ Supabase المجاني كل 4 أيام
- ✅ تذكير قبل انتهاء الدومين بـ 30 يوم
- ✅ تذكير بالعملاء اللي ما تواصلت معهم 60+ يوم
- ✅ نسخ احتياطي أسبوعي مشفر على الإيميل

### 🛠️ الراحة اليومية
- ✅ نسخ أي باسورد بضغطة بدون عرضه
- ✅ مولد باسوردات قوية مدمج
- ✅ تزامن فوري بين الموبايل والويب
- ✅ مشاركة مؤقتة آمنة (رابط يصلح ساعة واحدة)
- ✅ تصدير مشفر كـ backup

---

## 🤖 Health Check — كيف يعمل؟

```
Supabase Edge Function تعمل كل 4 أيام:

لكل مشروع:
├── يرسل ping → يتحقق إن Supabase شغال ✅
├── يتحقق من الموقع → يرجع 200 ✅
└── يتحقق من تواريخ الانتهاء ⚠️

النتيجة تظهر في Dashboard فوراً
وتشعرك إذا في مشكلة
```

---

## 🔗 المشاركة المؤقتة الآمنة

```
مثلاً احتجت تعطي شخص موثوق بيانات مشروع:

١. تختار المشروع
٢. تضغط "مشاركة مؤقتة"
٣. يولد رابط مشفر
٤. الرابط يصلح ساعة واحدة فقط
٥. يختفي تلقائياً ✅
```

---

## 📊 لوحة الإحصائيات

```
📈 هذا الشهر:
├── دخل: $X,XXX
├── مشاريع مكتملة: X
├── مشاريع نشطة: X
└── عملاء جدد: X

⚠️ تحتاج انتباه:
├── دومينات تنتهي خلال 30 يوم: X
├── فواتير معلقة: X
├── مشاريع Supabase اقتربت من الحد: X
└── عملاء لم تتواصل معهم 60+ يوم: X

🏷️ التقنيات الأكثر استخداماً:
├── Supabase: XX مشروع
├── Firebase: XX مشروع
├── React Native: XX مشروع
└── Next.js: XX مشروع
```

---

## 🗺️ خارطة التطوير

```
المرحلة ١ — الأساس (أسبوع ١-٢)
├── إعداد Expo + Supabase على Oracle
├── نظام Auth (إيميل + باسورد + TOTP)
└── قاعدة البيانات كاملة

المرحلة ٢ — الواجهة الأساسية (أسبوع ٣-٤)
├── Dashboard الرئيسي
├── إضافة / عرض / تعديل المشاريع
└── عرض بيانات الدخول مع 👁️

المرحلة ٣ — التشفير (أسبوع ٥)
├── AES-256 + IV لكل باسورد
├── PIN الرئيسي + Argon2
└── Shamir's Secret Sharing

المرحلة ٤ — الملفات والصور (أسبوع ٦)
├── رفع صور لكل مشروع
└── إدارة الملفات

المرحلة ٥ — الأتمتة (أسبوع ٧)
├── Health Check تلقائي
├── ping لـ Supabase المجاني
├── تذكيرات الدومين والاستضافة
└── نسخ احتياطي أسبوعي

المرحلة ٦ — المالية (أسبوع ٨)
├── تتبع الفواتير والمدفوعات
└── إحصائيات الدخل

المرحلة ٧ — الميزات المتقدمة (أسبوع ٩)
├── Timeout التلقائي
├── منع Screenshot
├── المشاركة المؤقتة الآمنة
└── مولد الباسوردات

المرحلة ٨ — الصقل والنشر (أسبوع ١٠)
├── اختبار كامل
├── PWA للويب
├── Build iOS + Android
└── توثيق كامل
```

---

## 💰 التكلفة الإجمالية

| الخدمة | التكلفة |
|--------|---------|
| Supabase على Oracle | **مجاني** |
| Expo | **مجاني** |
| Resend (إيميلات) | **مجاني** |
| Google Authenticator | **مجاني** |
| **المجموع** | **$0 / شهر** ✅ |

---

## 🚀 المستقبل — NexVault كـ SaaS

```
المرحلة الأولى:
└── أداة شخصية لك وحدك ✅

المرحلة الثانية:
└── تبيعه لمطورين آخرين
    ├── $9/شهر للفريلانسر
    ├── $29/شهر للفريق الصغير
    └── السوق: ملايين المطورين حول العالم

الفجوة في السوق:
└── لا يوجد منتج يجمع كل هذا في مكان واحد
    = فرصة حقيقية ✅
```

---

## 🔑 ملخص الأمان

```
بياناتك محمية بـ:

١. إيميل + باسورد قوي
٢. TOTP يتغير كل 30 ثانية
٣. PIN لا يُحفظ في السيرفر أبداً
٤. AES-256 بـ IV مختلف لكل باسورد
٥. Shamir لمنع ضياع المفتاح
٦. Timeout تلقائي بعد 5 دقائق
٧. منع Screenshot على الموبايل
٨. سجل كامل لكل النشاطات

= مستوى أمان البنوك — تكلفة صفر ✅
```

---

## 📝 ملاحظات التطوير

- كل باسورد له IV مختلف — حتى لو نفس الباسورد يظهر مشفراً مختلفاً
- PIN لا يُرسل للسيرفر أبداً — يبقى في الذاكرة فقط
- عند إغلاق التطبيق = PIN يختفي تلقائياً
- Shamir يحسب على الجهاز — لا يمر بالسيرفر

---

*وثيقة حية — الإصدار ٢.٠ — تُحدث مع تطور المشروع*
*NexVault © 2026 — خزنة المطور الاحترافية*
